<div class="cursor" id="cursor"></div>
<div class="cursor-ring" id="cursor-ring"></div>

<style>
  @media (hover: hover) and (pointer: fine) {
    body,
    body * {
      cursor: none !important;
    }
  }

  .cursor {
    width: 10px;
    height: 10px;
    position: fixed;
    top: 0;
    left: 0;
    pointer-events: none;
    z-index: 99999;
    border-radius: 999px;
    transform: translate3d(-50%, -50%, 0);
    transition:
      transform 0.18s ease,
      background 0.18s ease,
      box-shadow 0.18s ease,
      opacity 0.2s ease;
    opacity: 0;

    background: radial-gradient(
      circle at 30% 30%,
      rgba(255, 255, 255, 0.98) 0%,
      rgba(157, 212, 242, 0.94) 42%,
      rgba(65, 171, 226, 0.82) 100%
    );

    box-shadow:
      0 0 10px rgba(65, 171, 226, 0.28),
      0 0 20px rgba(157, 212, 242, 0.18);
  }

  .cursor::after {
    content: "";
    position: absolute;
    inset: 50% auto auto 50%;
    width: 56px;
    height: 56px;
    border-radius: 999px;
    transform: translate(-50%, -50%);
    background: radial-gradient(
      circle,
      rgba(65, 171, 226, 0.16) 0%,
      rgba(157, 212, 242, 0.08) 40%,
      transparent 74%
    );
    filter: blur(8px);
    pointer-events: none;
    z-index: -1;
    transition:
      transform 0.18s ease,
      opacity 0.18s ease,
      background 0.18s ease;
    opacity: 0.82;
  }

  .cursor-ring {
    width: 34px;
    height: 34px;
    position: fixed;
    top: 0;
    left: 0;
    pointer-events: none;
    z-index: 99998;
    border-radius: 999px;
    transform: translate3d(-50%, -50%, 0);
    transition:
      transform 0.2s ease,
      width 0.2s ease,
      height 0.2s ease,
      border-color 0.2s ease,
      box-shadow 0.2s ease,
      background 0.2s ease,
      opacity 0.2s ease;
    opacity: 0;

    border: 1px solid rgba(65, 171, 226, 0.35);
    background: linear-gradient(
      145deg,
      rgba(255, 255, 255, 0.14),
      rgba(157, 212, 242, 0.08)
    );

    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);

    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.35),
      0 0 12px rgba(65, 171, 226, 0.08);
  }

  .cursor.is-visible,
  .cursor-ring.is-visible {
    opacity: 1;
  }

  .cursor.hover {
    transform: translate3d(-50%, -50%, 0) scale(1.28);
    background: radial-gradient(
      circle at 30% 30%,
      rgba(255, 255, 255, 1) 0%,
      rgba(157, 212, 242, 0.96) 40%,
      rgba(65, 171, 226, 0.9) 100%
    );
    box-shadow:
      0 0 14px rgba(65, 171, 226, 0.34),
      0 0 24px rgba(157, 212, 242, 0.22);
  }

  .cursor.hover::after {
    transform: translate(-50%, -50%) scale(1.16);
    opacity: 1;
    background: radial-gradient(
      circle,
      rgba(65, 171, 226, 0.22) 0%,
      rgba(157, 212, 242, 0.12) 42%,
      transparent 76%
    );
  }

  .cursor-ring.hover {
    width: 48px;
    height: 48px;
    border-color: rgba(65, 171, 226, 0.58);
    background: linear-gradient(
      145deg,
      rgba(255, 255, 255, 0.18),
      rgba(157, 212, 242, 0.1)
    );
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.4),
      0 0 0 1px rgba(255, 255, 255, 0.12),
      0 0 22px rgba(65, 171, 226, 0.18);
  }

  @media (max-width: 768px), (hover: none), (pointer: coarse) {
    .cursor,
    .cursor-ring {
      display: none !important;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .cursor,
    .cursor-ring,
    .cursor::after {
      transition: none !important;
    }
  }
</style>
