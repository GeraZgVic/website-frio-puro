<section id="inicio" style="padding:0;">
  <div id="hero">
    <canvas id="particles-canvas"></canvas>
    <div class="hero-content">
      <div class="hero-left">
        <div class="hero-badge fade-in">⭐ #1 en Villahermosa Tabasco</div>
        <h1 class="hero-title fade-in fade-in-delay-1">
          HIELO<br /><span>PREMIUM</span><br />PARA TU<br />NEGOCIO
        </h1>
        <p class="hero-subtitle fade-in fade-in-delay-2">
          Distribuimos hielo de máxima calidad con entrega rápida y puntual.
          Ideal para restaurantes, bares, hoteles y eventos en todo
          Villahermosa, Tabasco.
        </p>
        <ul class="hero-checks">
          <li><span class="check-icon">✓</span> Entrega en menos de 2 horas</li>
          <li>
            <span class="check-icon">✓</span> Hielo 100% purificado y certificado
          </li>
          <li>
            <span class="check-icon">✓</span> Servicio disponible 24/7 los 365 días
          </li>
        </ul>
        <div class="hero-buttons fade-in fade-in-delay-3">
          <a href="https://wa.me/529611234567" class="btn-primary"
            >Solicitar Cotización</a
          >
          <a href="#productos" class="btn-outline">Ver Productos</a>
        </div>
      </div>

      <div class="hero-visual">
        <div class="ice-cube-container">
          <div class="ice-glow"></div>
          <span class="ice-emoji">🧊</span>
        </div>
      </div>
    </div>

    
  </div>
</section>

<style>
  #hero {
    min-height: 100vh;
    background: linear-gradient(
      135deg,
      var(--color-dark) 0%,
      var(--color-primary) 50%,
      var(--color-accent) 100%
    );
    position: relative;
    display: flex;
    align-items: center;
    overflow: hidden;
    padding: 120px 60px 80px;
    --nav-offset: 96px;
  }

  #particles-canvas {
    position: absolute;
    top: var(--nav-offset);
    left: 0;
    width: 100%;
    height: calc(100% - var(--nav-offset));
    pointer-events: none;
    z-index: 1;
  }

  .hero-content {
    position: relative;
    z-index: 2;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
    max-width: 1280px;
    margin: 0 auto;
    width: 100%;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: rgba(65, 171, 226, 0.2);
    border: 1px solid rgba(65, 171, 226, 0.5);
    color: var(--color-light-blue);
    padding: 8px 18px;
    border-radius: 50px;
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 28px;
    backdrop-filter: blur(8px);
  }

  .hero-title {
    font-family: "Barlow Condensed", sans-serif;
    font-weight: 800;
    font-size: clamp(3.5rem, 6vw, 6rem);
    color: var(--color-white);
    text-transform: uppercase;
    letter-spacing: -1px;
    line-height: 0.92;
    margin-bottom: 24px;
  }

  .hero-title span {
    color: var(--color-accent);
  }

  .hero-subtitle {
    font-size: 1.15rem;
    color: var(--color-light-blue);
    margin-bottom: 36px;
    line-height: 1.7;
    max-width: 480px;
  }

  .hero-checks {
    list-style: none;
    margin-bottom: 44px;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .hero-checks li {
    display: flex;
    align-items: center;
    gap: 12px;
    color: rgba(255, 255, 255, 0.9);
    font-weight: 600;
    font-size: 1rem;
    opacity: 0;
    transform: translateX(-20px);
    transition: all 0.5s ease;
  }

  .hero-checks li.visible {
    opacity: 1;
    transform: translateX(0);
  }

  .hero-checks li .check-icon {
    width: 24px;
    height: 24px;
    background: var(--color-accent);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    font-size: 0.75rem;
  }

  .hero-buttons {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
  }

  .hero-visual {
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  .ice-cube-container {
    position: relative;
    animation: float 4s ease-in-out infinite;
  }

  .ice-emoji {
    font-size: 14rem;
    display: block;
    filter: drop-shadow(0 0 60px rgba(65, 171, 226, 0.6))
      drop-shadow(0 0 120px rgba(65, 171, 226, 0.3));
    line-height: 1;
  }

  .ice-glow {
    position: absolute;
    width: 300px;
    height: 300px;
    background: radial-gradient(
      circle,
      rgba(65, 171, 226, 0.3) 0%,
      transparent 70%
    );
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    border-radius: 50%;
    animation: pulse-glow 3s ease-in-out infinite;
  }

  .scroll-indicator {
    position: absolute;
    bottom: 40px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    color: rgba(255, 255, 255, 0.5);
    font-size: 0.7rem;
    letter-spacing: 2px;
    text-transform: uppercase;
  }

  .scroll-indicator .mouse {
    width: 24px;
    height: 40px;
    border: 2px solid rgba(255, 255, 255, 0.3);
    border-radius: 12px;
    position: relative;
  }

  .scroll-indicator .mouse::after {
    content: "";
    width: 4px;
    height: 8px;
    background: var(--color-accent);
    border-radius: 2px;
    position: absolute;
    top: 6px;
    left: 50%;
    transform: translateX(-50%);
    animation: scroll-dot 1.8s ease-in-out infinite;
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0px) rotate(0deg);
    }

    25% {
      transform: translateY(-20px) rotate(3deg);
    }

    75% {
      transform: translateY(10px) rotate(-2deg);
    }
  }

  @keyframes pulse-glow {
    0%,
    100% {
      transform: translate(-50%, -50%) scale(1);
      opacity: 0.8;
    }

    50% {
      transform: translate(-50%, -50%) scale(1.3);
      opacity: 0.4;
    }
  }

  @keyframes scroll-dot {
    0% {
      top: 6px;
      opacity: 1;
    }

    100% {
      top: 22px;
      opacity: 0;
    }
  }

  @media (max-width: 1024px) {
    #hero {
      padding: 120px 40px 80px;
    }

    .hero-title {
      font-size: 3.5rem;
    }
  }

  @media (max-width: 768px) {
    #hero {
      padding: 100px 24px 80px;
    }

    .hero-content {
      grid-template-columns: 1fr;
    }

    .hero-visual {
      display: none;
    }
  }
</style>
