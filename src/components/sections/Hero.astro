---
import { buildWhatsAppUrl, commercialContent } from "../../config/commercial";
---

<section id="inicio" class="hero-section">
  <div id="hero">
    <canvas id="hero-particles-canvas"></canvas>

    <div class="hero-bg-glow hero-bg-glow-1"></div>
    <div class="hero-bg-glow hero-bg-glow-2"></div>

    <div class="hero-content">
      <div class="hero-left">
        <div class="hero-badge fade-in">
          <span class="hero-badge-dot"></span>
          #1 en Villahermosa, Tabasco
        </div>

        <h1 class="hero-title fade-in fade-in-delay-1">
          HIELO
          <span>PREMIUM</span>
          PARA TU NEGOCIO
        </h1>

        <p class="hero-subtitle fade-in fade-in-delay-2">
          Distribuimos hielo de máxima calidad con entrega rápida y puntual.
          Ideal para restaurantes, bares, hoteles y eventos en todo
          Villahermosa, Tabasco.
        </p>

        <ul class="hero-checks" aria-label="Beneficios principales">
          <li>
            <span class="check-icon">✓</span>
            <span>Entrega en menos de 2 horas</span>
          </li>
          <li>
            <span class="check-icon">✓</span>
            <span>Hielo 100% purificado y certificado</span>
          </li>
          <li>
            <span class="check-icon">✓</span>
            <span>{commercialContent.badges.service247}</span>
          </li>
        </ul>

        <div class="hero-buttons fade-in fade-in-delay-3">
          <a href={buildWhatsAppUrl()} class="btn-primary hero-cta-primary">
            {commercialContent.ctas.heroPrimary}
          </a>

          <a href="#productos" class="btn-outline hero-cta-secondary">
            Ver productos
          </a>
        </div>
      </div>

      <div class="hero-visual fade-in fade-in-delay-2">
        <div class="hero-visual-shell">
          <div class="hero-visual-aura"></div>
          <div class="hero-visual-ring hero-visual-ring-1"></div>
          <div class="hero-visual-ring hero-visual-ring-2"></div>

          <div class="hero-media-card">
            <div class="hero-media-shine"></div>
            <div class="hero-media-noise"></div>

            <div class="hero-media-frame">
              <img
                src="/images/Hero.png"
                alt="Hielo premium para negocio"
                class="hero-image"
                loading="eager"
                decoding="async"
                fetchpriority="high"
              />
            </div>

            <div class="hero-floating-card hero-floating-card-top">
              <span class="floating-label">Calidad</span>
              <strong>Premium</strong>
            </div>

            <div class="hero-floating-card hero-floating-card-bottom">
              <span class="floating-label">Cobertura</span>
              <strong>Entrega rápida</strong>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-section {
    padding: 0;
  }

  #hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    overflow: hidden;
    padding: 140px 60px 90px;
    isolation: isolate;
    background:
      radial-gradient(
        circle at 18% 22%,
        rgba(65, 171, 226, 0.22) 0%,
        transparent 32%
      ),
      radial-gradient(
        circle at 82% 30%,
        rgba(157, 212, 242, 0.12) 0%,
        transparent 24%
      ),
      linear-gradient(
        135deg,
        #171d48 0%,
        #232b68 32%,
        #2a4d8f 62%,
        #41abe2 100%
      );
  }

  #hero-particles-canvas {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 1;
  }

  .hero-bg-glow {
    position: absolute;
    border-radius: 999px;
    filter: blur(80px);
    pointer-events: none;
    z-index: 0;
  }

  .hero-bg-glow-1 {
    top: 8%;
    left: -4%;
    width: 340px;
    height: 340px;
    background: rgba(65, 171, 226, 0.18);
  }

  .hero-bg-glow-2 {
    right: -4%;
    bottom: 5%;
    width: 420px;
    height: 420px;
    background: rgba(157, 212, 242, 0.12);
  }

  .hero-content {
    position: relative;
    z-index: 2;
    display: grid;
    grid-template-columns: minmax(0, 1.02fr) minmax(420px, 0.98fr);
    gap: 64px;
    align-items: center;
    width: 100%;
    max-width: 1280px;
    margin: 0 auto;
  }

  .hero-left {
    position: relative;
    z-index: 2;
    max-width: 640px;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    min-height: 42px;
    padding: 10px 18px;
    margin-bottom: 28px;
    border-radius: 999px;
    border: 1px solid rgba(157, 212, 242, 0.28);
    background: linear-gradient(
      135deg,
      rgba(255, 255, 255, 0.12),
      rgba(255, 255, 255, 0.06)
    );
    backdrop-filter: blur(12px);
    color: var(--color-pale);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    box-shadow: 0 12px 40px rgba(5, 15, 50, 0.22);
  }

  .hero-badge-dot {
    width: 10px;
    height: 10px;
    border-radius: 999px;
    background: var(--color-accent);
    box-shadow: 0 0 0 6px rgba(65, 171, 226, 0.14);
    flex-shrink: 0;
  }

  .hero-title {
    margin-bottom: 24px;
    font-family: "Barlow Condensed", sans-serif;
    font-size: clamp(3.8rem, 7vw, 6.7rem);
    font-weight: 800;
    line-height: 0.88;
    letter-spacing: -1.8px;
    text-transform: uppercase;
    color: var(--color-white);
    text-wrap: balance;
    text-shadow: 0 10px 40px rgba(10, 16, 48, 0.28);
    width: 660px;
  }

  .hero-title span {
    display: inline-block;
    color: #8fd8ff;
    text-shadow:
      0 0 20px rgba(65, 171, 226, 0.22),
      0 0 44px rgba(65, 171, 226, 0.18);
  }

  .hero-subtitle {
    max-width: 560px;
    margin-bottom: 34px;
    font-size: 1.1rem;
    line-height: 1.8;
    color: rgba(224, 239, 248, 0.88);
  }

  .hero-checks {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 14px;
    margin-bottom: 40px;
  }

  .hero-checks li {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    color: rgba(255, 255, 255, 0.92);
    font-size: 1rem;
    font-weight: 600;
    line-height: 1.55;
    opacity: 0;
    transform: translateX(-20px);
    transition: all 0.5s ease;
  }

  .hero-checks li.visible {
    opacity: 1;
    transform: translateX(0);
  }

  .check-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 24px;
    height: 24px;
    margin-top: 1px;
    border-radius: 999px;
    flex-shrink: 0;
    background: linear-gradient(135deg, #41abe2, #7ad3ff);
    color: var(--color-white);
    font-size: 0.75rem;
    box-shadow:
      0 8px 20px rgba(65, 171, 226, 0.35),
      inset 0 1px 0 rgba(255, 255, 255, 0.28);
  }

  .hero-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    align-items: center;
  }

  .hero-cta-primary,
  .hero-cta-secondary {
    min-width: 220px;
    text-align: center;
  }

  .hero-visual {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 620px;
  }

  .hero-visual-shell {
    position: relative;
    width: min(100%, 620px);
    aspect-ratio: 1 / 1;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-visual-aura {
    position: absolute;
    width: 76%;
    height: 76%;
    border-radius: 999px;
    background: radial-gradient(
      circle,
      rgba(65, 171, 226, 0.32) 0%,
      rgba(65, 171, 226, 0.1) 40%,
      transparent 72%
    );
    filter: blur(14px);
    animation: heroAuraPulse 4.5s ease-in-out infinite;
    pointer-events: none;
  }

  .hero-visual-ring {
    position: absolute;
    border-radius: 999px;
    border: 1px solid rgba(157, 212, 242, 0.14);
    pointer-events: none;
  }

  .hero-visual-ring-1 {
    inset: 8%;
    animation: heroSpin 20s linear infinite;
  }

  .hero-visual-ring-2 {
    inset: 15%;
    border-style: dashed;
    animation: heroSpinReverse 28s linear infinite;
  }

  .hero-media-card {
    position: relative;
    width: min(100%, 520px);
    aspect-ratio: 1 / 1;
    border-radius: 38px;
    padding: 20px;
    overflow: visible;
    background: linear-gradient(
      145deg,
      rgba(255, 255, 255, 0.18),
      rgba(255, 255, 255, 0.05)
    );
    border: 1px solid rgba(255, 255, 255, 0.16);
    backdrop-filter: blur(16px);
    box-shadow:
      0 30px 80px rgba(5, 15, 50, 0.35),
      inset 0 1px 0 rgba(255, 255, 255, 0.16);
    animation: heroFloatCard 5.2s ease-in-out infinite;
  }

  .hero-media-shine {
    position: absolute;
    inset: 0;
    border-radius: inherit;
    pointer-events: none;
    background: linear-gradient(
      135deg,
      rgba(255, 255, 255, 0.16) 0%,
      rgba(255, 255, 255, 0.04) 30%,
      transparent 58%
    );
  }

  .hero-media-noise {
    position: absolute;
    inset: 0;
    border-radius: inherit;
    opacity: 0.06;
    pointer-events: none;
    background-image:
      radial-gradient(circle at 20% 20%, #ffffff 0.6px, transparent 0.7px),
      radial-gradient(circle at 80% 30%, #ffffff 0.6px, transparent 0.7px),
      radial-gradient(circle at 50% 75%, #ffffff 0.6px, transparent 0.7px);
    background-size:
      26px 26px,
      34px 34px,
      22px 22px;
  }

  .hero-media-frame {
    position: relative;
    z-index: 2;
    width: 100%;
    height: 100%;
    border-radius: 28px;
    overflow: hidden;
    background:
      radial-gradient(
        circle at 50% 40%,
        rgba(255, 255, 255, 0.2),
        transparent 55%
      ),
      linear-gradient(
        180deg,
        rgba(255, 255, 255, 0.08),
        rgba(255, 255, 255, 0.02)
      );
    border: 1px solid rgba(255, 255, 255, 0.14);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.12);
  }

  .hero-image {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
    object-position: center;
    transform: scale(1.03);
    filter: drop-shadow(0 22px 50px rgba(65, 171, 226, 0.18))
      drop-shadow(0 10px 20px rgba(10, 20, 55, 0.18));
  }

  .hero-floating-card {
    position: absolute;
    z-index: 3;
    min-width: 150px;
    padding: 14px 16px;
    border-radius: 18px;
    background: rgba(9, 21, 58, 0.55);
    border: 1px solid rgba(157, 212, 242, 0.18);
    backdrop-filter: blur(12px);
    box-shadow: 0 16px 32px rgba(4, 10, 35, 0.22);
  }

  .hero-floating-card strong {
    display: block;
    color: var(--color-white);
    font-size: 1rem;
    font-weight: 700;
    letter-spacing: 0.2px;
  }

  .floating-label {
    display: inline-block;
    margin-bottom: 4px;
    color: rgba(157, 212, 242, 0.88);
    font-size: 0.72rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1.2px;
  }

  .hero-floating-card-top {
    top: 24px;
    left: -26px;
    animation: heroFloatTag 4.2s ease-in-out infinite;
  }

  .hero-floating-card-bottom {
    right: -24px;
    bottom: 28px;
    animation: heroFloatTag 4.8s ease-in-out infinite 0.4s;
  }

  @keyframes heroFloatCard {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-14px);
    }
  }

  @keyframes heroFloatTag {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-8px);
    }
  }

  @keyframes heroAuraPulse {
    0%,
    100% {
      transform: scale(1);
      opacity: 0.9;
    }
    50% {
      transform: scale(1.08);
      opacity: 0.62;
    }
  }

  @keyframes heroSpin {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  @keyframes heroSpinReverse {
    from {
      transform: rotate(360deg);
    }
    to {
      transform: rotate(0deg);
    }
  }

  @media (max-width: 1200px) {
    #hero {
      padding: 132px 40px 84px;
    }

    .hero-content {
      grid-template-columns: minmax(0, 1fr) minmax(360px, 0.92fr);
      gap: 48px;
    }

    .hero-title {
      font-size: clamp(3.4rem, 6vw, 5.6rem);
    }

    .hero-visual {
      min-height: 560px;
    }

    .hero-media-card {
      width: min(100%, 470px);
    }
  }

  @media (max-width: 1024px) {
    #hero {
      min-height: auto;
      padding: 126px 32px 72px;
    }

    .hero-content {
      grid-template-columns: 1fr;
      gap: 34px;
    }

    .hero-left {
      max-width: 100%;
      text-align: center;
    }

    .hero-subtitle {
      max-width: 680px;
    }

    .hero-visual {
      min-height: auto;
      justify-content: center;
    }

    .hero-visual-shell {
      display: none;
    }
    .hero-title {
      text-align: center;
      margin-left: auto;
      margin-right: auto;
      max-width: 14ch;
      width: 100%;
    }
  }

  @media (min-width: 1025px) {
    .hero-title {
      max-width: 16ch;
    }

    .hero-title br {
      display: none;
    }
  }

  @media (max-width: 768px) {
    #hero {
      padding: 118px 24px 64px;
    }

    .hero-badge {
      font-size: 0.72rem;
      letter-spacing: 1.4px;
      padding: 9px 14px;
      margin-bottom: 22px;
    }

    .hero-title {
      font-size: clamp(3.2rem, 14vw, 4.9rem);
      line-height: 0.92;
      margin-bottom: 18px;
      text-align: center;
      letter-spacing: -1.4px;
      max-width: 14ch;
      width: 100%;
    }

    .hero-subtitle {
      font-size: 1rem;
      line-height: 1.75;
      margin-bottom: 28px;
    }

    .hero-checks {
      gap: 12px;
      margin-bottom: 30px;
    }

    .hero-checks li {
      font-size: 0.96rem;
    }

    .hero-buttons {
      flex-direction: column;
      align-items: stretch;
    }

    .hero-cta-primary,
    .hero-cta-secondary {
      width: 100%;
      min-width: 0;
    }

    .hero-visual {
      display: none;
    }

    .hero-visual-shell {
      width: min(100%, 430px);
    }

    .hero-media-card {
      width: 100%;
      padding: 14px;
      border-radius: 28px;
    }

    .hero-media-frame {
      border-radius: 20px;
    }

    .hero-floating-card {
      min-width: 128px;
      padding: 11px 13px;
      border-radius: 14px;
    }

    .hero-floating-card strong {
      font-size: 0.9rem;
    }

    .hero-floating-card-top {
      top: 10px;
      left: -8px;
    }

    .hero-floating-card-bottom {
      right: -8px;
      bottom: 14px;
    }
  }

  @media (max-width: 480px) {
    #hero {
      padding: 112px 18px 56px;
    }

    .hero-badge {
      width: 100%;
      justify-content: center;
    }

    .hero-title {
      font-size: clamp(3rem, 12.8vw, 3.9rem);
      letter-spacing: -1.2px;
      max-width: 15ch;
    }

    .hero-checks li {
      gap: 10px;
      font-size: 0.92rem;
    }

    .check-icon {
      width: 22px;
      height: 22px;
      font-size: 0.7rem;
    }

    .hero-visual-shell {
      width: 100%;
    }

    .hero-floating-card {
      min-width: 116px;
      padding: 10px 12px;
    }

    .floating-label {
      font-size: 0.66rem;
    }

    .hero-floating-card strong {
      font-size: 0.84rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-visual-aura,
    .hero-visual-ring-1,
    .hero-visual-ring-2,
    .hero-media-card,
    .hero-floating-card-top,
    .hero-floating-card-bottom {
      animation: none;
    }
  }
</style>
